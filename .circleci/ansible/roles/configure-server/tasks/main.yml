---
- name: "update apt packages."
  become: yes
  apt:
    update_cache: yes

- name: "upgrade packages"
  become: yes
  apt:
    upgrade: yes

- name: "install dependencies."
  become: yes
  apt:
    name: ["nodejs", "npm"]
    state: latest
    update_cache: yes

- name: ENV
  shell: |
    sudo export TEST=12
    sudo export TYPEORM_CONNECTION=$({{ lookup('env', 'TYPEORM_CONNECTION')}})
    sudo export TYPEORM_ENTITIES=$({{ lookup('env', 'TYPEORM_ENTITIES')}})
    sudo export TYPEORM_HOST=$({{ lookup('env', 'TYPEORM_HOST')}})
    sudo export TYPEORM_PORT=$({{ lookup('env', 'TYPEORM_PORT')}})
    sudo export TYPEORM_USERNAME=$({{ lookup('env', 'TYPEORM_USERNAME')}})
    sudo export TYPEORM_PASSWORD=$({{ lookup('env', 'TYPEORM_PASSWORD')}})
    sudo export TYPEORM_DATABASE=$({{ lookup('env', 'TYPEORM_DATABASE')}})
    sudo export TYPEORM_MIGRATIONS=$({{ lookup('env', 'TYPEORM_MIGRATIONS')}})
    sudo export TYPEORM_MIGRATIONS_DIR=$({{ lookup('env', 'TYPEORM_MIGRATIONS_DIR')}})
